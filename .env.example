# Local development placeholders only. Do not commit real .env files.

DJANGO_SETTINGS_MODULE=config.settings.local

DJANGO_SECRET_KEY=change-me-in-local-development
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_CSRF_TRUSTED_ORIGINS=

# PostgreSQL 14+ is the target database family for Django 5.2.
# These placeholder credentials assume a local database/user created separately.
DATABASE_URL=postgres://social_commerce:social_commerce@localhost:5432/social_commerce
TEST_DATABASE_NAME=test_social_commerce
